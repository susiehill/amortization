import React from 'react';
import { shallow } from 'enzyme';
import { Amort } from './Amort';

describe('An Amortization', () => {
  it('converts amount to numeric', () => {
    const component = shallow(<Amort />);
  }); 
});  